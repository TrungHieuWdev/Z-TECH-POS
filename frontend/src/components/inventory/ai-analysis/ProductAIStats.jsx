import { AlertTriangle, Boxes, CircleAlert, WalletCards } from 'lucide-react';
import { formatCurrency } from '../../../utils/format';
function Card({ icon: Icon, label, value, note, tone }) { return <article className="border border-gray-200 bg-white p-4 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-wide text-gray-500">{label}</p><p className="mt-2 text-2xl font-extrabold text-gray-950">{value}</p>{note && <p className="mt-1 text-xs font-medium text-gray-500">{note}</p>}</div><span className={`grid h-10 w-10 shrink-0 place-items-center ${tone}`}><Icon size={20} /></span></div></article>; }
export default function ProductAIStats({ products, totalAnalyzed = 0 }) { const high = products.filter((p) => p.alertLevel === 'high').length; const medium = products.filter((p) => p.alertLevel === 'medium').length; const value = products.reduce((sum, p) => sum + p.inventoryValue, 0); return <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Card icon={Boxes} label="Tổng sản phẩm phân tích" value={totalAnalyzed.toLocaleString('vi-VN')} tone="bg-sky-50 text-sky-700"/><Card icon={CircleAlert} label="Cảnh báo cao" value={high} tone="bg-red-50 text-red-700"/><Card icon={AlertTriangle} label="Cảnh báo trung bình" value={medium} tone="bg-amber-50 text-amber-700"/><Card icon={WalletCards} label="Vốn tồn của SP cảnh báo" value={formatCurrency(value)} note="Tồn hiện tại × giá nhập" tone="bg-emerald-50 text-emerald-700"/></div>; }
