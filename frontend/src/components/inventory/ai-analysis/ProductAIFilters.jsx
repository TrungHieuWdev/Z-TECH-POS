import { ALERT_LEVELS, ANALYSIS_PERIODS, PHONE_BRANDS } from '../../../constants/productAIAnalysisConstants';
export default function ProductAIFilters({ filters, onChange, categories = [] }) { const field = 'h-10 border border-gray-300 bg-white px-3 text-sm text-gray-700 outline-none focus:border-[#69afd6]'; return <div className="grid gap-2 sm:grid-cols-2 xl:grid-cols-4"><select value={filters.days} onChange={(e) => onChange({ ...filters, days: Number(e.target.value) })} className={field}>{ANALYSIS_PERIODS.map((days) => <option key={days} value={days}>{days} ngày</option>)}</select><select value={filters.categoryId} onChange={(e) => onChange({ ...filters, categoryId: e.target.value })} className={field}><option value="">Tất cả danh mục</option>{categories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select><select value={filters.phoneBrandId} onChange={(e) => onChange({ ...filters, phoneBrandId: e.target.value })} className={field}><option value="">Tất cả dòng máy / Model</option>{PHONE_BRANDS.map((item) => <option key={item.value} value={item.value}>{item.label}</option>)}</select><select value={filters.alertLevel} onChange={(e) => onChange({ ...filters, alertLevel: e.target.value })} className={field}><option value="">Tất cả mức cảnh báo</option>{ALERT_LEVELS.map((item) => <option key={item.value} value={item.value}>{item.label}</option>)}</select></div>; }
