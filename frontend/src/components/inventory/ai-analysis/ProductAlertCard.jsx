import { Eye, TicketPercent } from 'lucide-react';
import ProductImage from '../../ProductImage';
import { formatCurrency } from '../../../utils/format';
import ProductAlertBadge from './ProductAlertBadge';
export default function ProductAlertCard({ product, onCreatePromotion, onView }) { return <article className="border border-gray-200 bg-white p-4 shadow-sm"><div className="grid gap-4 xl:grid-cols-[88px_minmax(220px,1.1fr)_minmax(420px,1.8fr)_160px]"><ProductImage product={product} className="h-20 w-20" compact/><div className="min-w-0"><div className="flex items-start justify-between gap-2"><h3 className="font-bold text-gray-950">{product.name}</h3><ProductAlertBadge level={product.alertLevel}/></div><p className="mt-1 text-xs font-semibold text-[#398fbd]">{product.sku || product.barcode || 'Chưa có SKU/Barcode'}</p><p className="mt-2 text-xs text-gray-500">{product.category_name || 'Chưa phân loại'} · {product.device_model || product.device_family || 'Dùng chung'}</p></div><div><div className="grid grid-cols-2 gap-x-5 gap-y-2 text-sm sm:grid-cols-5"><Info label="Tồn hiện tại" value={product.stock_quantity}/><Info label="Tồn tối thiểu" value={product.min_stock || 0}/><Info label="Đã bán" value={product.sold_quantity}/><Info label="Giá nhập" value={formatCurrency(product.cost_price || 0)}/><Info label="Giá trị tồn" value={formatCurrency(product.inventoryValue)}/></div><div className="mt-3 border-t border-gray-100 pt-3 text-xs leading-5"><p className="text-gray-700"><b>Nhận xét:</b> {product.analysisComment}</p><p className="mt-1 text-sky-800"><b>Gợi ý:</b> {product.actionSuggestion}</p></div></div><div className="flex flex-col justify-center gap-2"><button type="button" onClick={() => onCreatePromotion(product)} className="flex h-10 items-center justify-center gap-2 bg-[#69afd6] px-3 text-sm font-bold text-white hover:bg-[#579fc8]"><TicketPercent size={16}/>Tạo khuyến mãi</button><button type="button" onClick={() => onView(product)} className="flex h-10 items-center justify-center gap-2 border border-[#9fd4ed] bg-white px-3 text-sm font-bold text-[#398fbd] hover:bg-sky-50"><Eye size={16}/>Xem chi tiết</button></div></div></article>; }
function Info({ label, value }) { return <div><p className="text-xs font-extrabold uppercase tracking-wide text-gray-600">{label}</p><p className="mt-1 text-base font-extrabold text-gray-950">{value}</p></div>; }
